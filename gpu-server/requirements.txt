opencv-python-headless>=4.9.0
torch>=2.1.0
torchvision>=0.16.0
# >=8.2.0 required for YOLO('yolov8m.pt') auto-download and updated .boxes API
ultralytics>=8.2.0
numpy>=1.26.0,<2.0.0
# >=0.21.0 required for ByteTrack.update_with_detections signature change;
# <0.30.0 because ByteTrack (our fallback tracker) is deprecated in 0.28 and
# removed in 0.30
supervision>=0.21.0,<0.30.0
runpod>=1.6.2
boto3>=1.34.0
requests>=2.31.0
scipy>=1.11.4
filterpy>=1.4.5
lap>=0.4.0
easyocr>=1.7.1
SoccerNet>=0.1.50
# boxmot (BoT-SORT + OSNet re-ID + camera-motion compensation) is installed
# in a tolerant separate Dockerfile step — see gpu-server/Dockerfile.
# v4: tracking re-ID, jersey OCR overhaul, goal detection
